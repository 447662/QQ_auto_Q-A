# QQ_auto_Q-A 项目规则

## 全局 AI 自动回复门禁

本项目内任何面向 QQ、QQ群、QQ 频道、帖子、评论、私信或其他外部消息的 AI 自动回答，都必须先执行以下门禁：

1. 只有消息中包含明确启动口令 `#配置推荐` 时，才允许进入 AI 回答流程。
2. 去掉 `#配置推荐` 后，剩余内容必须是电脑配置相关问题，例如大学新生购机、笔记本/台式机选购、DIY 装机、硬件兼容性、预算配置、游戏/AI/仿真性能、电商比价或购买渠道。
3. 未包含 `#配置推荐`，或包含口令但内容不是电脑配置相关问题时，只能返回固定文本：`该功能尚未实现，请询问群主或者管理员`。
4. 通过门禁后，也只能回答电脑配置相关内容；不得扩展为通用问答、闲聊、学校政策、课程答疑、生活咨询、群规解释、其他商品推荐等。
5. 电商检索、比价、PDF 生成、QQ/频道发送等技能只能作为通过门禁后的电脑配置推荐流程的辅助能力；不得因单独出现商品、链接、PDF、QQ、频道等关键词而绕过门禁。

上述规则约束项目产出的自动问答行为；用户明确要求进行项目维护、代码修改、技能修改、调试、提交等开发操作时，可以按开发任务正常执行。
